# Project Journal — 2026-07

## 2026-07-26

- **[process]** Switched YDB from a single flat `IMPROVEMENT_PLAN.md` to a `docs/` + ADR + subagent
  structure, matching the exlibris/personal-brand workspace convention. *Why: `IMPROVEMENT_PLAN.md`'s
  M1–M7 rework is fully merged (no open work), and a new epic (YNAB migration) needs a home that isn't
  a doc scoped to a finished UI/reliability project.* `IMPROVEMENT_PLAN.md` archived to
  `docs/archive/`. See `AGENTS.md` for the new workspace overview.
- **[decision]** Scoped a YNAB→YDB migration integration: one-way pull, manual-first (ADR-0001,
  ADR-0002), amending `IMPROVEMENT_PLAN.md` §4's blanket "no bank API integrations" non-goal for this
  specific, bounded case. *Why: Shyam wants to stop paying for YNAB but needs a dual-running
  transition period to trust YDB first.*
- **[ops]** Created the "YDB Migration — Tickets" Notion Kanban board, nested under the existing
  "ydb" page, and seeded it with 5 Phase 1 tickets + 1 dormant Phase 2 placeholder. Data source ID
  recorded in `AGENTS.md` § MCP connections.
- **[decision]** YNAB token storage: `.env` var (`YNAB_ACCESS_TOKEN`, `YNAB_BUDGET_ID`), not
  Settings-stored — recorded in `docs/architecture.md`. Token verified against YNAB's `/budgets`
  endpoint; budget confirmed as "Shyam x Super" (`5f79b7ed-de54-43c8-940f-a1fcab0746fe`). Both values
  are set in `.env` (gitignored).
- **[decision]** Superseded the original account-mapping approach: instead of mapping YNAB accounts
  onto YDB's existing ones, **fully reset YDB's ledger data** (Accounts, Transactions, Categories,
  VendorRules, Budgets, ImportRecords) and recreate accounts fresh to mirror YNAB 1:1. See
  `docs/adr/0003-full-reset-before-ynab-import.md`. *Why: Shyam confirmed existing YDB data isn't
  precious — YNAB is the trusted source of truth for the migration period, and the two account lists
  don't map cleanly (YNAB has per-person sub-accounts YDB doesn't).* **Not yet executed** — reset is
  sequenced to happen right before the first real import runs, once the import pipeline exists, not
  immediately.
- **[ops]** Started `docs/research/ynab-vs-ydb/` — a Karpathy-style findings wiki logging where
  YNAB's data model differs from YDB's, so those differences can inform YDB's design later. First
  finding logged: YNAB's per-person sub-accounts (Wallet - Shyam / Wallet - Super).
- **[scope]** The SMS→Signal→YNAB capture pipeline (previously "outside this repo, already decided
  separately") now has a home: a new sibling repo, `expense-capture`
  (`~/Documents/Projects/expense-capture`), scaffolded the same session with its own docs/ADRs. See
  that repo's `docs/adr/` for the Signal-over-Telegram and shared-group-routing decisions.
- **[scope]** Built Phase 1 "Import from YNAB" pipeline on branch `ynab-import-pipeline`: `lib/ynab.ts`
  (API client), `lib/ynabImport.ts` (shared preview/commit logic including transfer pairing), three new
  routes under `app/api/ynab/`, `YnabImportManager` Settings component, and `Transaction.ynabId` column
  for idempotency. Covers all transaction types (income, expense, and internal YNAB transfers). Typechecked,
  22 new unit tests all passing, not yet merged.
- **[decision]** Opening balance handling: YNAB's "Starting Balance" transactions (one per account) are
  imported as regular transactions, and every account recreated during the ledger reset (ADR-0003) must
  have `openingBalance: 0`, so YNAB's row is the sole source of each account's opening balance in YDB.
  Documented as a load-bearing requirement in `docs/adr/0003-full-reset-before-ynab-import.md` and
  `docs/research/ynab-vs-ydb/findings.md`.
- **[refactor]** Fixed a silent correctness bug: YNAB's real incremental-sync parameter is
  `last_knowledge_of_server`, not `server_knowledge` — the latter is silently ignored and returns full
  history, so every "incremental" sync would have rescanned all data with no error. Corrected in
  `lib/ynab.ts`.
- **[ops]** Logged YNAB API shape surprises to `docs/research/ynab-vs-ydb/findings.md`: split
  transactions expose legs as `subtransactions[]`, category names can be emoji-prefixed and include
  YNAB's control buckets (`Inflow: Ready to Assign`, `Split`), and `payee_name` is nullable (importer
  falls back to memo, then a placeholder).
- **[refactor]** Fixed import pipeline robustness: delta cursor no longer advances past skipped rows
  (preventing data loss when a row is unmapped, incomplete, or ADR-0004-blocked), same-account transfer
  pairs pre-filtered in `planYnabImport`, and `$transaction` timeout bumped to 60s for full-history
  first imports. Reuse of pre-computed `accountCurrencies` map avoids re-querying on commit. Tests
  expanded from 22 to 34; added full-Prisma idempotency integration test (`planYnabImport` → commit
  cycle on fresh DB). → `docs/ynab-import-fix-plan.md`.
- **[decision]** ADR-0004: YNAB mutations (edits/deletions since last import) are detected by content
  fingerprint against the stored row and reported for manual reconciliation, never auto-applied or
  auto-deleted. Implemented across `lib/ynab.ts` (tombstones returned), `lib/ynabImport.ts` (plan
  counters), `app/api/ynab/import/route.ts` (commit-path handling), and YnabImportManager UI
  (expandable report blocks). See `docs/adr/0004-ynab-mutations-detect-and-report.md`.
- **[decision]** ADR-0005 (supersedes ADR-0004's detection mechanism): `@qa` found that comparing a
  freshly-pulled YNAB row against the live YDB row produced permanent false positives — any local
  ledger edit (e.g. recategorizing, the most common thing Shyam will do) made every future import
  report that row as "changed in YNAB" and froze the delta cursor for good. Fixed with a new
  `Transaction.ynabFingerprint` column: a versioned snapshot of YNAB-native fields only, written once
  at import time and never touched by any other write path, compared against the fresh pull instead
  of the live row. A local edit is now invisible to detection by construction. See
  `docs/adr/0005-ynab-change-detection-immutable-snapshot.md`.
- **[fix]** `@qa` also found a silent-wrong-balance bug outside the original fix plan: deleting one
  leg of an imported transfer (YDB's `DELETE` only cascade-deletes a locally-created counterpart,
  never an imported one) left an orphan leg that a re-import miscounted as "already imported" rather
  than detecting the missing side — permanently masking a broken balance. `planYnabImport` now
  reports this distinctly as `orphanedTransferLegs`, never as a duplicate, and holds the cursor until
  Shyam resolves it by hand. → `docs/ynab-import-fix-plan.md`.
- **[scope]** Code review (commit 39a5189) found and fixed a perf regression: new `accountCurrencies`
  cache param had accidentally serialized two currency lookups (manual/bulk transfer writes) that should
  run in parallel. Restored with per-side cache check; tests remain at 280 passing. → `docs/ynab-import-fix-plan.md`.
- **[ops]** Smoke-tested Phase 1 pipeline end-to-end: applied pending `ynabFingerprint` migration to
  dev.db, verified Prisma Studio showed the new column, Settings → Import from YNAB UI rendered,
  manual transfer validated/saved cleanly.
- **[ops]** Merged `ynab-import-pipeline` to `main` (commit 39a5189, clean fast-forward) and pushed to
  `origin/main`. Cleaned up 5 local branches (claude/fix-audit-findings, four worktree-agent-*) and 5
  remote branches (origin/claude/m8a-upcoming-bills, m8b-budget-ux, m8c-rules-retro-apply, m8d-ledger-keyboard,
  ynab-import-pipeline). Repo now has only `main` locally and remotely, both in sync. **Phase 1 of the
  YNAB migration is now live and fully tested** — all 280 tests passing, reviewed, and QA-verified for
  the two blocking correctness bugs (half-deleted-transfer and ADR-0004 false positives, both resolved).

## 2026-07-29

- **[ops]** Shyam updated the `expense-capture` sibling repo (SMS→Signal→YNAB capture pipeline) and ran
  Phase 1 "Import from YNAB" to pull the latest data into YDB. YNAB and YDB are now in sync. *Why:
  This is the start of daily-use activity feeding the Phase 1→2 gate — per ADR-0002, Phase 1 (manual
  import) must run for a few weeks with no mis-mapped transactions before Shyam declares the trigger
  to start Phase 2 (automatic sync) work.* See `AGENTS.md` Phase 1→2 gate checklist and
  `docs/adr/0002-manual-import-before-auto-sync.md`.

- **[ops]** Added historian Stop hook (`.claude/hooks/historian-check.py`) ported from exlibris/personal-brand sibling repos with YDB-adapted watch list (canonical docs + config files, excluding `docs/journal/`). Uses git-ignored `.claude/.historian-last-seen` marker (advanced by historian after logging, not by hook — prevents self-triggering) and wired to `.claude/settings.json`.

- **[scope]** Cleaned `FOLLOWUPS.md` for accuracy — marked 3 pre-existing follow-ups as shipped (reimbursement-suggest UI in `13fc673`, transfer-cascade softening in `6cb3e7e`, 2/3 DB constraints in `6cb3e7e`). Moved open `linkedTransferId` symmetry constraint to Notion board ticket. Added parked prompt-injection hardening (trigger: external contributor access; mirrors OPS-SEC-01 in sibling repos).

- **[decision]** Added ADR-0006 (`docs/adr/0006-local-only-llm-inference.md`, Accepted, 2026-07-29), recording the zero-egress invariant for YDB's chat and extraction paths (self-hosted Ollama only, no hosted LLM API). *Why: The property was load-bearing for ADR-0001's scoping but had never been explicitly stated as an invariant, risking someone adding a hosted fallback for perceived quality gain.* ADR-0006 rejects that idea (would ship transaction narration or query intent off-LAN for unmeasured benefit). Updated `docs/architecture.md`'s canonical-invariants table and indexed in `docs/adr/README.md`; noted an Open Question about the missing chat eval harness.

- **[scope]** Authored `docs/chat-knowledge-content-outline.md` (chat-knowledge ticket 1 deliverable): detailed outline of 21 financial-hygiene snippets for the chat assistant, with location-aware constraints flagging UAE-specific differences (no personal income tax, no public safety net for expats, job-loss consequences for visa status). Tickets 2–4 will draft content, decide storage location, and wire into chat prompts. See handoffs section for sequencing (P0/P1/P2 priorities).

- **[scope]** Delivered `docs/chat-knowledge-content.md` (chat-knowledge ticket 2): drafted all 21 snippets (P0/P1/P2 priority tiers) at 40–80 words each, tightened from ticket estimate to fit prompt-budget constraints on the narration call. Includes source notes verifying UAE-specific regulatory details (ILOE scheme, early-settlement caps, card mechanics). Handoff notes for ticket 4 specify budget sizing and P2 measurement gates. → `docs/chat-knowledge-content.md`.

- **[decision]** Planned six new tickets (Ollama/local-model brainstorm, Phase "Other"): SQL-guard false positives with budget aggregate aliases, golden-query eval harness, splitting chatModel into sqlModel+narrationModel, grammar/JSON-schema constrained SQL generation, units determinism in narration, and prompt-injection review of transaction-description inputs. Bundled with a follow-up ticket for a broader "what could we get from our chat model(s)?" brainstorm and a knowledge-base-ingestion pipeline ticket. → Notion board "YDB Migration — Tickets" (board ID fe3ba716-d772-48be-acaf-20601c1638c3).

- **[scope]** Scoped a private-financial-dossier initiative (knowledge-base brainstorm): yielded a 3-ticket chain ([private-kb] Architecture & isolation ADR, local-only harness/infrastructure, local-model eval harness) plus an explicitly-deferred milestone ("revoke Claude Code access to YDB DB + YNAB API when mature" + relocating prisma/dev.db outside repo). Key finding: DB isolation is not a new KB problem (ledger already in repo), it is about the authored/narrative tier (income, goals, plans) YDB doesn't yet know. → Notion board.

- **[process]** Updated `@historian` agent definition (`.claude/agents/historian.md`) to formalize Notion-sourced entries as part of historian's remit. *Why: Stop hook has no visibility into Notion writes, so planning sessions that exist only on the board (new initiatives, multi-ticket batches, deferred milestones) would otherwise leave no trail for future sessions to discover from git alone.* Entry pattern follows the same "pointer, not copy" rule as canonical-doc entries, pointing to the Notion board rather than duplicating ticket text.

- **[scope]** Shipped chat-knowledge ticket 3: defined storage location (`docs/knowledge/`) and file naming convention (`<id>-<kebab-slug>.md`) for 25 financial-hygiene knowledge snippets with YAML front-matter schema (id, title, area, priority, status, words, keywords, plus sources/verified on the 5 UAE-web-verified items). *Why: Knowledge content is application input (injected into chat prompts), not human-only reference — this is the first `docs/` directory that requires version control like code, so edits must now go through branch+PR.* Injection rule: snippet body up to first `##` heading; content after that is human-only notes. Fixed a count error inherited from ticket 2: the set is actually 25 snippets / 12 P0 (not "22 / 13 P0"), with P0 total at 737 words. Updated `AGENTS.md` to note the change in `docs/` workflow. See `docs/knowledge/README.md`; ticket 4 (chat pipeline wiring) remains gated on @tech-lead sign-off. → Commit `cc90d58`.

- **[decision]** @tech-lead cleared the design gate for chat-knowledge ticket 4 ("Design + implement knowledge injection into chat pipeline"): ADR-0007 (Accepted, 2026-07-29) specifies knowledge snippets inject into the narration prompt only, never the SQL-generation prompt. *Why: The SQL step's prompt gets billed twice on a repair round-trip (route.ts:137 and :176), so adding ~1,000 tokens of prose there costs 2,000 tokens vs. 1,000 in narration. Narration is where the framing is actually used anyway.* Verified the read-only SQL guard (`lib/prisma.ts`) remains unaffected on both sides — it is input-agnostic, inspecting only the generated SQL string at its two call sites, both fed from `generateSql`. Loading: read P0 snippets (status: active, priority: P0) fresh at request time, 12 files, no caching. Scope change: dropped the retrieval/filter function ticket 4 originally called for — ticket 3 sized P0 specifically so flat injection is affordable with no retrieval layer needed. Token budget: ~1,000 tokens per turn accepted; implementation must set `num_ctx` explicitly or verify Ollama's resolved default covers the worst case (prompt truncates from the front where the knowledge sits). Seven implementation conditions logged on Notion ticket (id 3aba225a-2512-81a0-ad0f-fc39c34c5144), status left at "Not started" (design gate cleared, implementation hasn't run). Sequencing note: ticket [chat-bug] (cents-vs-dollars narration determinism) edits the same narration system prompt string this work touches — recommend landing that first if near-term. Updated `docs/architecture.md` (new "Chat pipeline and the read-only guard" section + 3 Open Questions), `docs/adr/README.md`, and `AGENTS.md` canonical-decisions table. → `docs/adr/0007-chat-knowledge-injected-into-narration-only.md`.

- **[decision]** @tech-lead investigated two correctness bugs surfaced by manual chat testing (2026-07-29): (1) "How much did I spend on groceries?" returned a silent "no data" because the LLM guessed a category literal with no match in stored values; (2) "How long could I cover expenses?" produced self-contradictory totals from mixing asset and liability account balances without applying the `lib/accounts.ts` sign rule. Authored ADR-0008 and ADR-0009 as decision records; both fix on the generation side, not the guard: ADR-0008 injects real `Transaction.category` vocabulary into the SQL prompt at request time and fails loudly on unmatched categories (rejecting fuzzy matching as a new silent-wrongness mode); ADR-0009 declares `Account.openingBalance` off-limits to generated SQL and declines balance/net-worth questions until a `computeBalance`-backed path exists (rejects the alternative of duplicating sign-rule arithmetic in a prompt). Updated `AGENTS.md` canonical-decisions table with two new rows pointing to the ADRs and `docs/adr/README.md` index. Refined `docs/architecture.md` to reframe the read-only guard as a safety boundary (not a correctness one, since both bugs were well-formed queries) and added three new Open Questions (unowned eval harness, pre-SQL scope-check latency trade-off, and account-name grounding). Implementation is separate ticket work for `@backend-engineer`. → `docs/adr/0008-chat-sql-category-vocabulary-grounding.md`, `docs/adr/0009-balance-composition-out-of-scope-for-chat-sql.md`.

- **[decision]** @tech-lead authored a three-ADR architectural vision for chat (committed as `c71d00e`) in response to Shyam's direct product feedback: "chat doesn't figure out what it needs, doesn't check its own work, and I trust the ledger more." Phased plan (not urgent, over time): **Phase A (buildable now):** verification pass — one extra model call between SQL execution and narration reviews result shape against the question and gates narration on a structured verdict (passes `ok`, refuses on `no-data`, `out-of-scope`, etc.; see ADR-0014), produces missing eval data as a side effect. **Phase B (dormant):** real tool-calling loop on Ollama's `/api/chat` endpoint under bounded ReAct-style budgets (4-call limit, row budget, wall-clock cap), letting model inspect its own results before retrying or answering; ADR-0007's narration-only knowledge injection gets revisited here since loop collapses the current two-prompt structure (left as open question for Phase B). **Phase C (dormant):** code-computed tools (`get_balances` via `computeBalance`, `list_accounts`, `list_categories`) finally answering balance questions. *Why phasing: tool-calling is available today (measured: Ollama 0.30.6, qwen2.5:32b supports it, ~7s per turn) so capability isn't the gate; the gate is having an eval harness before expanding the route that sits on the read-only guard.* **Key finding from live testing:** tech-lead ran a null-result test (groceries category as literal, no match) and confirmed model noticed failure and retried, but retried by adding a date filter instead of questioning the wrong literal — the architecture win is the model *looking* before committing, not that more iteration makes it reason better. Same test produced unquoted `Transaction` reference and lowercase category guess, confirming ADR-0008/0010/0011 point fixes remain necessary and complementary to the architecture change (defense-in-depth, not made redundant). Realistic target assessment: open-ended multi-hop reasoning from a local 32B model isn't achievable regardless of phasing; the realistic goal is "reliable, checkable, willing to decline rather than eloquent" which is what Shyam wants anyway. Eval harness is now blocking four separate decisions and should be built before Phase A ships. Updated `AGENTS.md` canonical-decisions table (three new rows), `docs/architecture.md` (new "Chat pipeline architecture roadmap" section + revised Open Questions), and `docs/adr/README.md`. → `docs/adr/0012-agentic-chat-loop-target-architecture.md`, `docs/adr/0013-verification-pass-before-tool-calling-loop.md`, `docs/adr/0014-non-answer-is-a-first-class-chat-response.md`.

- **[decision]** ADR-0009 superseded by ADR-0010 and ADR-0011 after @backend-engineer's feasibility review surfaced a flaw: `openingBalance: 0` for all YNAB-imported accounts meant ADR-0009's ban wouldn't fix the observed bugs. @tech-lead then pulled persisted `ChatMessage.sql` rows from prior test sessions and found: (1) the "job loss" repro was never a balance bug—it was a UNION/compound SELECT query where SQLite names results by the first branch only, silently discarding the second alias, so both rows came back mislabeled `total_expenses`; (2) there was a separate real balance bug (session 10, "which debt should I pay off first") where the SQL summed one month's transactions and aliased it `total_balance`, but never referenced `openingBalance` at all, meaning ADR-0009's enforcement would have had zero effect. ADR-0010 moves balance-semantics enforcement from input-side column (`openingBalance` ban) to output-side alias rejection—route now rejects result columns claiming balance semantics (names like `balance`, `net_worth`, `outstanding`, `owed`). ADR-0011 bans `UNION`/compound `SELECT`s entirely, fixing the session-11 mislabeling bug by requiring multi-metric answers as multiple aliased columns in one row instead. Both ADRs note residual gap: aliases like `total` still evade the check; standing answer is a proper `computeBalance`-backed code path. Rewritten `docs/architecture.md` chat pipeline section with revised Open Questions; updated `AGENTS.md` canonical-decisions table + `docs/adr/README.md` index. → `docs/adr/0010-balance-semantics-enforced-on-result-label.md`, `docs/adr/0011-chat-sql-no-union-compound-selects.md`.

- **[scope]** @product-manager scoped "Chat SQL correctness (separate initiative)" — a new red phase on the Notion Kanban board ("YDB Migration — Tickets", data source `fe3ba716-d772-48be-acaf-20601c1638c3`), following the precedent of the existing "Chat knowledge" initiative. Six `[chat-sql]` tickets filed: legible refusal-response shapes (API + ChatPane, shared prerequisite for later tickets), `num_ctx` pinning on SQL-gen Ollama call, category + account-name vocabulary grounding (ADR-0008), balance-semantics enforcement on result labels (ADR-0010), compound-SELECT ban (ADR-0011), and regression fixtures from 2026-07-29 test sessions. Tickets 0–4 owned by @backend-engineer, ticket 5 (@qa); two pre-existing tickets flagged as collision risks (one affected by ADR-0011's scope, one by ADR-0010's alias detection). *Why: ADR-0008/0010/0011 each fix a distinct root cause of observed chat-SQL correctness bugs.* This work is explicitly outside YNAB Phase 1→2 sequencing. → Notion board link in `AGENTS.md`.

- **[ops]** Merged PR #25 ("Add a legible no-answer chat response (ADR-0014)") to main (commit 8153ddd, fast-forward from 55fc851). Implements the first half of the chat architecture roadmap: three-frame NDJSON (narration, sql, **no-answer**). New `no-answer` frame type with structured reasons (`no-data`, `unsupported-shape`), persisted to `ChatMessage.nonAnswerReason` column and migration. ChatPane now renders refusals with the explanation as headline, attempted SQL expanded by default, and nonAnswerReason survives reload — fixing the prior bug where silent "no data" got narrated as zero. New "Chat wire format" section added to `docs/architecture.md`. This is ticket "[chat-sql] 0", the pull-first prerequisite unblocking tickets 1–4. → `docs/adr/0014-non-answer-is-a-first-class-chat-response.md`.

- **[scope]** Live testing of PR #29 ("Ground chat SQL in the stored category vocabulary", ADR-0008) before merge uncovered a gap one level deeper than the original bug: asking "What was spent on Sports in July?" (not a real category) produced a confident wrong answer by substituting `'Uncategorized'`—a real, grounded value that passed the guard's check. Root cause was a prompt contradiction leaving no legal move for "nothing corresponds." Fixed with a reserved sentinel (`__no_matching_category__`) that the prompt is now instructed to use when nothing matches, backed by a worked example and explicit prohibition on catch-all substitution. The existing groundedness guard rejects the sentinel with zero new detection code. Verified live against Ollama (qwen2.5:32b) with the real dev-DB vocabulary (46 categories): pre-fix generated `category = 'Uncategorized'`; post-fix, identical setup, generated the sentinel and was correctly refused. → ADR-0008 addendum ("Addendum (2026-07-29): real-category substitution").

## 2026-07-30

- **[decision]** ADR-0015 supersedes ADR-0010. Balance-scope enforcement moves upstream from SQL-generation time (column alias blocklist) to question-time (intent check before any SQL is generated). *Why: Live testing found ADR-0010's alias check insufficient — narration also reads the user's question and uses that to frame its answer, so a SUM aliased `net` was confidently narrated as a car-loan balance anyway.* ADR-0015 refuses stock-noun questions ("balance", "net worth", "owe", "outstanding", etc.) before SQL generation happens, with ADR-0010's two checks (`openingBalance` + alias rejection) demoted to a secondary net. `@backend-engineer` implementing the question-check on PR #30 alongside PR #30's existing alias implementation. → `docs/adr/0015-balance-scope-enforced-on-the-question.md`, Supersedes `docs/adr/0010-balance-semantics-enforced-on-result-label.md`.

- **[process]** Added explicit branch-verification steps to `@tech-lead` and `@historian` agent definitions. *Why: A shared working directory let @tech-lead commit an ADR while on a feature branch but report it as landed on main, which @historian then trusted and journaled as live. Both agents now run `git branch --show-current` before any "commit to main" action and verify the result.* → `.claude/agents/tech-lead.md` (§5 → §6, new rule), `.claude/agents/historian.md` (new rules 10–11).

- **[decision]** ADR-0011 and ADR-0014 addenda (commit ef52718): ADR-0011's compound-SELECT ban widened from `UNION`/`UNION ALL` to all three SQLite compound operators (`UNION`, `UNION ALL`, `INTERSECT`, `EXCEPT`), preemptively rather than waiting for a live failure; the first-branch-column-naming defect is a documented SQLite rule and the failure class is silent/money-denominated. ADR-0014 addendum settles the `out-of-scope` vs. `unsupported-shape` discriminator: the former applies when the *question* cannot be answered from this ledger, the latter when the *generated query* is untrustworthy. ADR-0011's compound-SELECT rejection is therefore `unsupported-shape`, not `out-of-scope`. *Why discriminator: Drift per-ticket risked inconsistency across guards; a single rule (question vs. artifact) covers all four non-answer reasons.* → `docs/adr/0011-chat-sql-no-union-compound-selects.md` (addendum § 71–106), `docs/adr/0014-non-answer-is-a-first-class-chat-response.md` (addendum § 69–102).

## 2026-07-31

- **[refactor]** PR #32 fixed two transfer-handling bugs in chat SQL via prompt-code guards, found via live testing. First: income/expense aggregates double-counted transfer legs (matched pairs stored as equal/opposite become both income and expense, inflating both totals by transfer volume); fixed with `transactionType != 'transfer'` guard on sign-split queries and worked example. Second: transfer-volume naive sums returned ~0 (opposite-signed pairs cancel); fixed with worked example using `SUM(CASE WHEN amount > 0 THEN amount ELSE 0 END)` to sum inflow legs only. Both follow the existing split-leg/reimbursement guard pattern (prompt-rule, no ADR/route-detector). → Commit a82f2d0; tests: `tests/chatTransferExclusion.test.ts`, `tests/chatTransferVolume.test.ts`.

- **[process]** PR #32 privacy incident and remediation: real financial figures (Shyam's actual income/expense totals from dev DB) leaked into the PR description, commit message, and test-file comment. Caught by automated security-review flag before merge; redacted and force-pushed to clean up. Going forward: agents documenting bugs found via live personal financial data must describe bugs by shape/magnitude only—never literal figures—in any content landing in public PRs/commits/issues. YDB repo is public. → Remediated pre-merge in PR #32.

- **[decision]** Broader inconsistency surfaced and filed as Notion ticket: `SQL_SYSTEM_PROMPT`'s worked examples apply transfer/split-leg/reimbursement guards inconsistently—some carry all applicable guards, others omit one or more. (Surfaced twice today from PR #32 transfer bugs and ADR-0011 review note.) Assigned to @tech-lead for architecture scoping: prompt-only fix vs. independent route-level detector, following ADR-0008/0010/0011 pattern. → Notion ticket `[chat-sql] 6`; marked "not ready to pull" pending design gate.

