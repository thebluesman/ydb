# Project Journal — 2026-07

## 2026-07-26

- **[process]** Switched YDB from a single flat `IMPROVEMENT_PLAN.md` to a `docs/` + ADR + subagent
  structure, matching the exlibris/personal-brand workspace convention. *Why: `IMPROVEMENT_PLAN.md`'s
  M1–M7 rework is fully merged (no open work), and a new epic (YNAB migration) needs a home that isn't
  a doc scoped to a finished UI/reliability project.* `IMPROVEMENT_PLAN.md` archived to
  `docs/archive/`. See `AGENTS.md` for the new workspace overview.
- **[decision]** Scoped a YNAB→YDB migration integration: one-way pull, manual-first (ADR-0001,
  ADR-0002), amending `IMPROVEMENT_PLAN.md` §4's blanket "no bank API integrations" non-goal for this
  specific, bounded case. *Why: Shyam wants to stop paying for YNAB but needs a dual-running
  transition period to trust YDB first.*
- **[ops]** Created the "YDB Migration — Tickets" Notion Kanban board, nested under the existing
  "ydb" page, and seeded it with 5 Phase 1 tickets + 1 dormant Phase 2 placeholder. Data source ID
  recorded in `AGENTS.md` § MCP connections.
- **[decision]** YNAB token storage: `.env` var (`YNAB_ACCESS_TOKEN`, `YNAB_BUDGET_ID`), not
  Settings-stored — recorded in `docs/architecture.md`. Token verified against YNAB's `/budgets`
  endpoint; budget confirmed as "Shyam x Super" (`5f79b7ed-de54-43c8-940f-a1fcab0746fe`). Both values
  are set in `.env` (gitignored).
- **[decision]** Superseded the original account-mapping approach: instead of mapping YNAB accounts
  onto YDB's existing ones, **fully reset YDB's ledger data** (Accounts, Transactions, Categories,
  VendorRules, Budgets, ImportRecords) and recreate accounts fresh to mirror YNAB 1:1. See
  `docs/adr/0003-full-reset-before-ynab-import.md`. *Why: Shyam confirmed existing YDB data isn't
  precious — YNAB is the trusted source of truth for the migration period, and the two account lists
  don't map cleanly (YNAB has per-person sub-accounts YDB doesn't).* **Not yet executed** — reset is
  sequenced to happen right before the first real import runs, once the import pipeline exists, not
  immediately.
- **[ops]** Started `docs/research/ynab-vs-ydb/` — a Karpathy-style findings wiki logging where
  YNAB's data model differs from YDB's, so those differences can inform YDB's design later. First
  finding logged: YNAB's per-person sub-accounts (Wallet - Shyam / Wallet - Super).
- **[scope]** The SMS→Signal→YNAB capture pipeline (previously "outside this repo, already decided
  separately") now has a home: a new sibling repo, `expense-capture`
  (`~/Documents/Projects/expense-capture`), scaffolded the same session with its own docs/ADRs. See
  that repo's `docs/adr/` for the Signal-over-Telegram and shared-group-routing decisions.
- **[scope]** Built Phase 1 "Import from YNAB" pipeline on branch `ynab-import-pipeline`: `lib/ynab.ts`
  (API client), `lib/ynabImport.ts` (shared preview/commit logic including transfer pairing), three new
  routes under `app/api/ynab/`, `YnabImportManager` Settings component, and `Transaction.ynabId` column
  for idempotency. Covers all transaction types (income, expense, and internal YNAB transfers). Typechecked,
  22 new unit tests all passing, not yet merged.
- **[decision]** Opening balance handling: YNAB's "Starting Balance" transactions (one per account) are
  imported as regular transactions, and every account recreated during the ledger reset (ADR-0003) must
  have `openingBalance: 0`, so YNAB's row is the sole source of each account's opening balance in YDB.
  Documented as a load-bearing requirement in `docs/adr/0003-full-reset-before-ynab-import.md` and
  `docs/research/ynab-vs-ydb/findings.md`.
- **[refactor]** Fixed a silent correctness bug: YNAB's real incremental-sync parameter is
  `last_knowledge_of_server`, not `server_knowledge` — the latter is silently ignored and returns full
  history, so every "incremental" sync would have rescanned all data with no error. Corrected in
  `lib/ynab.ts`.
- **[ops]** Logged YNAB API shape surprises to `docs/research/ynab-vs-ydb/findings.md`: split
  transactions expose legs as `subtransactions[]`, category names can be emoji-prefixed and include
  YNAB's control buckets (`Inflow: Ready to Assign`, `Split`), and `payee_name` is nullable (importer
  falls back to memo, then a placeholder).
- **[refactor]** Fixed import pipeline robustness: delta cursor no longer advances past skipped rows
  (preventing data loss when a row is unmapped, incomplete, or ADR-0004-blocked), same-account transfer
  pairs pre-filtered in `planYnabImport`, and `$transaction` timeout bumped to 60s for full-history
  first imports. Reuse of pre-computed `accountCurrencies` map avoids re-querying on commit. Tests
  expanded from 22 to 34; added full-Prisma idempotency integration test (`planYnabImport` → commit
  cycle on fresh DB). → `docs/ynab-import-fix-plan.md`.
- **[decision]** ADR-0004: YNAB mutations (edits/deletions since last import) are detected by content
  fingerprint against the stored row and reported for manual reconciliation, never auto-applied or
  auto-deleted. Implemented across `lib/ynab.ts` (tombstones returned), `lib/ynabImport.ts` (plan
  counters), `app/api/ynab/import/route.ts` (commit-path handling), and YnabImportManager UI
  (expandable report blocks). See `docs/adr/0004-ynab-mutations-detect-and-report.md`.
- **[decision]** ADR-0005 (supersedes ADR-0004's detection mechanism): `@qa` found that comparing a
  freshly-pulled YNAB row against the live YDB row produced permanent false positives — any local
  ledger edit (e.g. recategorizing, the most common thing Shyam will do) made every future import
  report that row as "changed in YNAB" and froze the delta cursor for good. Fixed with a new
  `Transaction.ynabFingerprint` column: a versioned snapshot of YNAB-native fields only, written once
  at import time and never touched by any other write path, compared against the fresh pull instead
  of the live row. A local edit is now invisible to detection by construction. See
  `docs/adr/0005-ynab-change-detection-immutable-snapshot.md`.
- **[fix]** `@qa` also found a silent-wrong-balance bug outside the original fix plan: deleting one
  leg of an imported transfer (YDB's `DELETE` only cascade-deletes a locally-created counterpart,
  never an imported one) left an orphan leg that a re-import miscounted as "already imported" rather
  than detecting the missing side — permanently masking a broken balance. `planYnabImport` now
  reports this distinctly as `orphanedTransferLegs`, never as a duplicate, and holds the cursor until
  Shyam resolves it by hand. → `docs/ynab-import-fix-plan.md`.
- **[scope]** Code review (commit 39a5189) found and fixed a perf regression: new `accountCurrencies`
  cache param had accidentally serialized two currency lookups (manual/bulk transfer writes) that should
  run in parallel. Restored with per-side cache check; tests remain at 280 passing. → `docs/ynab-import-fix-plan.md`.
- **[ops]** Smoke-tested Phase 1 pipeline end-to-end: applied pending `ynabFingerprint` migration to
  dev.db, verified Prisma Studio showed the new column, Settings → Import from YNAB UI rendered,
  manual transfer validated/saved cleanly.
- **[ops]** Merged `ynab-import-pipeline` to `main` (commit 39a5189, clean fast-forward) and pushed to
  `origin/main`. Cleaned up 5 local branches (claude/fix-audit-findings, four worktree-agent-*) and 5
  remote branches (origin/claude/m8a-upcoming-bills, m8b-budget-ux, m8c-rules-retro-apply, m8d-ledger-keyboard,
  ynab-import-pipeline). Repo now has only `main` locally and remotely, both in sync. **Phase 1 of the
  YNAB migration is now live and fully tested** — all 280 tests passing, reviewed, and QA-verified for
  the two blocking correctness bugs (half-deleted-transfer and ADR-0004 false positives, both resolved).

## 2026-07-29

- **[ops]** Shyam updated the `expense-capture` sibling repo (SMS→Signal→YNAB capture pipeline) and ran
  Phase 1 "Import from YNAB" to pull the latest data into YDB. YNAB and YDB are now in sync. *Why:
  This is the start of daily-use activity feeding the Phase 1→2 gate — per ADR-0002, Phase 1 (manual
  import) must run for a few weeks with no mis-mapped transactions before Shyam declares the trigger
  to start Phase 2 (automatic sync) work.* See `AGENTS.md` Phase 1→2 gate checklist and
  `docs/adr/0002-manual-import-before-auto-sync.md`.

- **[ops]** Added historian Stop hook (`.claude/hooks/historian-check.py`) ported from exlibris/personal-brand sibling repos with YDB-adapted watch list (canonical docs + config files, excluding `docs/journal/`). Uses git-ignored `.claude/.historian-last-seen` marker (advanced by historian after logging, not by hook — prevents self-triggering) and wired to `.claude/settings.json`.

- **[scope]** Cleaned `FOLLOWUPS.md` for accuracy — marked 3 pre-existing follow-ups as shipped (reimbursement-suggest UI in `13fc673`, transfer-cascade softening in `6cb3e7e`, 2/3 DB constraints in `6cb3e7e`). Moved open `linkedTransferId` symmetry constraint to Notion board ticket. Added parked prompt-injection hardening (trigger: external contributor access; mirrors OPS-SEC-01 in sibling repos).

- **[decision]** Added ADR-0006 (`docs/adr/0006-local-only-llm-inference.md`, Accepted, 2026-07-29), recording the zero-egress invariant for YDB's chat and extraction paths (self-hosted Ollama only, no hosted LLM API). *Why: The property was load-bearing for ADR-0001's scoping but had never been explicitly stated as an invariant, risking someone adding a hosted fallback for perceived quality gain.* ADR-0006 rejects that idea (would ship transaction narration or query intent off-LAN for unmeasured benefit). Updated `docs/architecture.md`'s canonical-invariants table and indexed in `docs/adr/README.md`; noted an Open Question about the missing chat eval harness.

- **[scope]** Authored `docs/chat-knowledge-content-outline.md` (chat-knowledge ticket 1 deliverable): detailed outline of 21 financial-hygiene snippets for the chat assistant, with location-aware constraints flagging UAE-specific differences (no personal income tax, no public safety net for expats, job-loss consequences for visa status). Tickets 2–4 will draft content, decide storage location, and wire into chat prompts. See handoffs section for sequencing (P0/P1/P2 priorities).

- **[scope]** Delivered `docs/chat-knowledge-content.md` (chat-knowledge ticket 2): drafted all 21 snippets (P0/P1/P2 priority tiers) at 40–80 words each, tightened from ticket estimate to fit prompt-budget constraints on the narration call. Includes source notes verifying UAE-specific regulatory details (ILOE scheme, early-settlement caps, card mechanics). Handoff notes for ticket 4 specify budget sizing and P2 measurement gates. → `docs/chat-knowledge-content.md`.

- **[decision]** Planned six new tickets (Ollama/local-model brainstorm, Phase "Other"): SQL-guard false positives with budget aggregate aliases, golden-query eval harness, splitting chatModel into sqlModel+narrationModel, grammar/JSON-schema constrained SQL generation, units determinism in narration, and prompt-injection review of transaction-description inputs. Bundled with a follow-up ticket for a broader "what could we get from our chat model(s)?" brainstorm and a knowledge-base-ingestion pipeline ticket. → Notion board "YDB Migration — Tickets" (board ID fe3ba716-d772-48be-acaf-20601c1638c3).

- **[scope]** Scoped a private-financial-dossier initiative (knowledge-base brainstorm): yielded a 3-ticket chain ([private-kb] Architecture & isolation ADR, local-only harness/infrastructure, local-model eval harness) plus an explicitly-deferred milestone ("revoke Claude Code access to YDB DB + YNAB API when mature" + relocating prisma/dev.db outside repo). Key finding: DB isolation is not a new KB problem (ledger already in repo), it is about the authored/narrative tier (income, goals, plans) YDB doesn't yet know. → Notion board.

- **[process]** Updated `@historian` agent definition (`.claude/agents/historian.md`) to formalize Notion-sourced entries as part of historian's remit. *Why: Stop hook has no visibility into Notion writes, so planning sessions that exist only on the board (new initiatives, multi-ticket batches, deferred milestones) would otherwise leave no trail for future sessions to discover from git alone.* Entry pattern follows the same "pointer, not copy" rule as canonical-doc entries, pointing to the Notion board rather than duplicating ticket text.

- **[scope]** Shipped chat-knowledge ticket 3: defined storage location (`docs/knowledge/`) and file naming convention (`<id>-<kebab-slug>.md`) for 25 financial-hygiene knowledge snippets with YAML front-matter schema (id, title, area, priority, status, words, keywords, plus sources/verified on the 5 UAE-web-verified items). *Why: Knowledge content is application input (injected into chat prompts), not human-only reference — this is the first `docs/` directory that requires version control like code, so edits must now go through branch+PR.* Injection rule: snippet body up to first `##` heading; content after that is human-only notes. Fixed a count error inherited from ticket 2: the set is actually 25 snippets / 12 P0 (not "22 / 13 P0"), with P0 total at 737 words. Updated `AGENTS.md` to note the change in `docs/` workflow. See `docs/knowledge/README.md`; ticket 4 (chat pipeline wiring) remains gated on @tech-lead sign-off. → Commit `cc90d58`.
